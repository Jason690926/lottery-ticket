# 自動帶入最新/上一期號碼 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 威力彩 + 大樂透分頁開啟時自動把最新一期號碼帶入第一選號區塊、上一期帶入第二選號區塊，並自動產生分析結果；使用者手改後不被覆蓋，提供「↻ 帶回最新期」按鈕。

**Architecture:** 採方案 A。`query_engine.py` 新增 `latest_two_draws(cfg)`（自帶含 `draw_id` 的查詢，不動共用的 `_get_draws`）。`ui_shared.py` 新增 `seed_inputs(cfg, key_prefix)`（draw_id 哨兵機制，僅在沒種過或新一期時寫 widget 的 `st.session_state`）、`reset_seed(key_prefix)`（on_click callback 清哨兵）、`render_seed_caption(info)`。`ui_powerball.render()` / `ui_lotto649.render()` 各插一次 `seed_inputs` 呼叫、改寫按鈕列加重設鈕、加 caption。分析邏輯與資料層零改動。

**Tech Stack:** Python 3.14（`.venv/`）、Streamlit 1.57、SQLite、pandas、`streamlit.testing.v1.AppTest`（專案無 pytest）。指令以 PowerShell + `.venv\Scripts\` 為準。

**Spec:** `docs/superpowers/specs/2026-05-16-自動帶入號碼-design.md`

**測試策略：** 無 pytest 框架。引擎用 `.venv\Scripts\python.exe -c` ad-hoc 比對 DB；UI 用一次性 `_seed_verify.py`（AppTest，驗證後刪除、不 commit，沿用本專案 sanity check 慣例）。

---

## Task 1：`query_engine.latest_two_draws`

**Files:**
- Modify: `query_engine.py`（檔尾新增函式，不動既有函式）

- [ ] **Step 1: 寫驗證腳本（先失敗）**

Create `_lt2_verify.py`:

```python
"""一次性驗證 latest_two_draws；驗證後刪除、不 commit。"""
import sqlite3
from query_engine import latest_two_draws, POWERBALL, LOTTO649, DB_PATH

for cfg in (POWERBALL, LOTTO649):
    latest, prev, latest_id = latest_two_draws(cfg)
    conn = sqlite3.connect(DB_PATH)
    rows = conn.execute(
        f"SELECT draw_id, draw_date, n1,n2,n3,n4,n5,n6, n_zone2 "
        f"FROM {cfg.table} ORDER BY id DESC LIMIT 2"
    ).fetchall()
    conn.close()

    db_latest = rows[0]
    assert latest is not None, f"{cfg.name}: latest 不應為 None"
    assert latest["draw_id"] == str(db_latest[0]), (cfg.name, latest["draw_id"], db_latest[0])
    assert latest["draw_date"] == db_latest[1], (cfg.name, latest["draw_date"], db_latest[1])
    assert latest["z1"] == [int(x) for x in db_latest[2:8]], (cfg.name, latest["z1"])
    assert latest["z2"] == int(db_latest[8]), (cfg.name, latest["z2"])
    assert latest_id == latest["draw_id"]

    db_prev = rows[1]
    assert prev is not None, f"{cfg.name}: prev 不應為 None（DB 有 1000+ 期）"
    assert prev["draw_id"] == str(db_prev[0]), (cfg.name, prev["draw_id"], db_prev[0])
    assert prev["z1"] == [int(x) for x in db_prev[2:8]], (cfg.name, prev["z1"])
    print(f"  {cfg.name}: latest={latest['draw_id']} prev={prev['draw_id']} OK")

print("latest_two_draws ALL OK")
```

- [ ] **Step 2: 跑驗證，確認失敗**

Run: `.venv\Scripts\python.exe _lt2_verify.py`
Expected: `ImportError: cannot import name 'latest_two_draws' from 'query_engine'`

- [ ] **Step 3: 實作 latest_two_draws**

在 `query_engine.py` 檔案**最末**（`zone2_analysis` 之後）加入：

```python
def _draw_row_to_dict(r) -> dict:
    """pandas Series（一列開獎）→ {draw_id, draw_date, z1, z2}。"""
    return {
        "draw_id": str(r["draw_id"]),
        "draw_date": r["draw_date"],
        "z1": [int(r[c]) for c in Z1_COLS],
        "z2": int(r["n_zone2"]),
    }


def latest_two_draws(
    cfg: LotteryConfig = POWERBALL,
) -> tuple[dict | None, dict | None, str]:
    """
    回傳 (最新期, 上一期, 最新 draw_id)。
    每個 dict: {"draw_id": str, "draw_date": str, "z1": [n1..n6], "z2": int}
    - 無資料：   (None, None, "")
    - 只有 1 期：(最新, None, latest_id)
    - 2 期以上： (最新, 上一期, latest_id)
    自帶含 draw_id 的查詢（_get_draws 不含 draw_id，且為分析引擎共用，
    不在此擴充以免影響已 sanity-check 的行為）。
    """
    conn = sqlite3.connect(DB_PATH)
    df = pd.read_sql(
        f"SELECT draw_id, draw_date, n1,n2,n3,n4,n5,n6, n_zone2 "
        f"FROM {cfg.table} ORDER BY id DESC LIMIT 2",
        conn,
    )
    conn.close()

    if df.empty:
        return None, None, ""

    latest = _draw_row_to_dict(df.iloc[0])
    prev = _draw_row_to_dict(df.iloc[1]) if len(df) > 1 else None
    return latest, prev, latest["draw_id"]
```

- [ ] **Step 4: 跑驗證，確認通過**

Run: `.venv\Scripts\python.exe _lt2_verify.py`
Expected:
```
  powerball: latest=... prev=... OK
  lotto649: latest=... prev=... OK
latest_two_draws ALL OK
```

- [ ] **Step 5: 刪除驗證腳本（不 commit）**

Run: `Remove-Item _lt2_verify.py`

- [ ] **Step 6: Commit**

```bash
git add query_engine.py
git commit -m "feat(engine): 新增 latest_two_draws（取最新/上一期，含 draw_id）"
```

---

## Task 2：`ui_shared` 種子化 helper

**Files:**
- Modify: `ui_shared.py`（新增 import 與 3 個函式，不動既有元件）

- [ ] **Step 1: 更新 import**

把 `ui_shared.py` 頂部：

```python
from query_engine import ROLLING, zone1_analysis, zone1_dual_combined, zone2_analysis
```

改為：

```python
from query_engine import (
    ROLLING, zone1_analysis, zone1_dual_combined, zone2_analysis,
    latest_two_draws,
)
```

- [ ] **Step 2: 在 `ui_shared.py` 檔尾新增 3 個函式**

```python
# ── 自動帶入最新/上一期號碼（draw_id 哨兵機制）────────────────────────────────
def seed_inputs(cfg, key_prefix: str) -> tuple[str, str, str, str]:
    """
    在 widget 建立『之前』呼叫。依 draw_id 哨兵決定是否種子化 4 個 widget。
    僅在「沒種過」或「最新 draw_id 改變（新一期）」時寫入 session_state，
    否則 no-op（保留使用者手改值）。
    回傳 (latest_id, latest_date, prev_id, prev_date)；無資料回 ("","","","")。
    """
    latest, prev, latest_id = latest_two_draws(cfg)
    p = key_prefix
    if latest is None:
        return ("", "", "", "")

    if st.session_state.get(f"{p}_seeded_id") != latest_id:
        st.session_state[f"{p}_b1_z1"] = list(latest["z1"])
        st.session_state[f"{p}_b1_z2"] = latest["z2"]
        st.session_state[f"{p}_b2_z1"] = list(prev["z1"]) if prev else []
        st.session_state[f"{p}_b2_z2"] = prev["z2"] if prev else None
        st.session_state[f"{p}_seeded_id"] = latest_id

    return (
        latest_id,
        latest["draw_date"],
        prev["draw_id"] if prev else "",
        prev["draw_date"] if prev else "",
    )


def reset_seed(key_prefix: str) -> None:
    """重設按鈕的 on_click callback：清哨兵，下一輪 seed_inputs 會重新種子。"""
    st.session_state.pop(f"{key_prefix}_seeded_id", None)


def render_seed_caption(info: tuple[str, str, str, str]) -> None:
    """顯示帶入提示。info 為 seed_inputs 的回傳值。"""
    latest_id, latest_date, prev_id, prev_date = info
    if not latest_id:
        st.caption("⚠️ 尚無開獎資料，未自動帶入。")
        return
    msg = f"🔄 已自動帶入最新一期 {latest_id}（{latest_date}）為第一區塊"
    if prev_id:
        msg += f"、上一期 {prev_id}（{prev_date}）為第二區塊"
    msg += "。手動修改後可按「↻ 帶回最新期」復原。"
    st.caption(msg)
```

- [ ] **Step 3: smoke import 測試**

Run: `.venv\Scripts\python.exe -c "from ui_shared import seed_inputs, reset_seed, render_seed_caption; print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add ui_shared.py
git commit -m "feat(ui): ui_shared 新增 seed_inputs / reset_seed / render_seed_caption"
```

---

## Task 3：整合進威力彩與大樂透分頁

**Files:**
- Modify: `ui_powerball.py`（import、seed_inputs 呼叫、按鈕列、caption）
- Modify: `ui_lotto649.py`（同上，前綴 lt）

- [ ] **Step 1: ui_powerball.py — 更新 import**

把 `ui_powerball.py` 的：

```python
from ui_shared import (
    PALETTE_B1, PALETTE_B2,
    render_block_section, render_combined, render_zone2_section,
)
```

改為：

```python
from ui_shared import (
    PALETTE_B1, PALETTE_B2,
    render_block_section, render_combined, render_zone2_section,
    seed_inputs, reset_seed, render_seed_caption,
)
```

- [ ] **Step 2: ui_powerball.py — 在 render() 開頭呼叫 seed_inputs**

把：

```python
def render():
    cfg = POWERBALL

    st.markdown(
        f'<div class="sub-title">POWERBALL CONDITIONAL FREQUENCY ANALYZER　｜　'
```

改為：

```python
def render():
    cfg = POWERBALL
    seed_info = seed_inputs(cfg, "pb")

    st.markdown(
        f'<div class="sub-title">POWERBALL CONDITIONAL FREQUENCY ANALYZER　｜　'
```

- [ ] **Step 3: ui_powerball.py — 改寫按鈕列、加 caption**

把：

```python
    st.markdown("")
    _, btn_col, _ = st.columns([3, 2, 3])
    with btn_col:
        run = st.button("🔍　開始分析", use_container_width=True, type="primary", key="pb_run")
```

改為：

```python
    render_seed_caption(seed_info)
    st.markdown("")
    _, c_run, c_reset, _ = st.columns([2, 2, 2, 2])
    with c_run:
        run = st.button("🔍　開始分析", use_container_width=True, type="primary", key="pb_run")
    with c_reset:
        st.button("↻　帶回最新期", use_container_width=True, key="pb_reset",
                  on_click=reset_seed, args=("pb",))
```

- [ ] **Step 4: ui_lotto649.py — 更新 import**

把 `ui_lotto649.py` 的：

```python
from ui_shared import (
    PALETTE_B1, PALETTE_B2,
    render_block_section, render_combined, render_zone2_section,
)
```

改為：

```python
from ui_shared import (
    PALETTE_B1, PALETTE_B2,
    render_block_section, render_combined, render_zone2_section,
    seed_inputs, reset_seed, render_seed_caption,
)
```

- [ ] **Step 5: ui_lotto649.py — 在 render() 開頭呼叫 seed_inputs**

把：

```python
def render():
    cfg = LOTTO649

    st.markdown(
        f'<div class="sub-title">LOTTO 6/49 CONDITIONAL FREQUENCY ANALYZER　｜　'
```

改為：

```python
def render():
    cfg = LOTTO649
    seed_info = seed_inputs(cfg, "lt")

    st.markdown(
        f'<div class="sub-title">LOTTO 6/49 CONDITIONAL FREQUENCY ANALYZER　｜　'
```

- [ ] **Step 6: ui_lotto649.py — 改寫按鈕列、加 caption**

把（注意大樂透按鈕列前面有「特別號分析說明」info-card，不要動它）：

```python
    _, btn_col, _ = st.columns([3, 2, 3])
    with btn_col:
        run = st.button("🔍　開始分析", use_container_width=True, type="primary", key="lt_run")
```

改為：

```python
    render_seed_caption(seed_info)
    _, c_run, c_reset, _ = st.columns([2, 2, 2, 2])
    with c_run:
        run = st.button("🔍　開始分析", use_container_width=True, type="primary", key="lt_run")
    with c_reset:
        st.button("↻　帶回最新期", use_container_width=True, key="lt_reset",
                  on_click=reset_seed, args=("lt",))
```

- [ ] **Step 7: py_compile + import smoke**

Run:
```
.venv\Scripts\python.exe -m py_compile app.py ui_powerball.py ui_lotto649.py ui_shared.py query_engine.py
.venv\Scripts\python.exe -c "import ui_powerball, ui_lotto649; print('imports OK')"
```
Expected: 無錯誤、`imports OK`

- [ ] **Step 8: Commit**

```bash
git add ui_powerball.py ui_lotto649.py
git commit -m "feat(ui): 威力彩/大樂透分頁整合自動帶入 + 重設按鈕 + caption"
```

---

## Task 4：AppTest 端到端驗證 + 收尾

**Files:**
- Create（暫存，不 commit）: `_seed_verify.py`
- Modify: `CLAUDE.md`（進度快照）

- [ ] **Step 1: 寫 AppTest 驗證腳本**

Create `_seed_verify.py`:

```python
"""一次性 AppTest 端到端驗證；驗證後刪除、不 commit。"""
import sqlite3
from streamlit.testing.v1 import AppTest
from query_engine import POWERBALL, LOTTO649, DB_PATH


def db_two(cfg):
    conn = sqlite3.connect(DB_PATH)
    rows = conn.execute(
        f"SELECT draw_id, n1,n2,n3,n4,n5,n6, n_zone2 "
        f"FROM {cfg.table} ORDER BY id DESC LIMIT 2"
    ).fetchall()
    conn.close()
    return rows


pb = db_two(POWERBALL)
lt = db_two(LOTTO649)
pb_latest_z1 = [int(x) for x in pb[0][1:7]]
pb_latest_z2 = int(pb[0][7])
pb_prev_z1 = [int(x) for x in pb[1][1:7]]
lt_latest_z1 = [int(x) for x in lt[0][1:7]]

# 1) 全新啟動：4 個 widget 被種子化為最新/上一期
at = AppTest.from_file("app.py", default_timeout=90).run()
assert not at.exception, at.exception
assert at.session_state["pb_b1_z1"] == pb_latest_z1, at.session_state["pb_b1_z1"]
assert at.session_state["pb_b1_z2"] == pb_latest_z2, at.session_state["pb_b1_z2"]
assert at.session_state["pb_b2_z1"] == pb_prev_z1, at.session_state["pb_b2_z1"]
assert at.session_state["lt_b1_z1"] == lt_latest_z1, at.session_state["lt_b1_z1"]
assert at.session_state["pb_seeded_id"] == str(pb[0][0])
print("  [1] 全新啟動種子化 OK")

# 2) 手改後重跑（哨兵相符）→ 值保留
at.session_state["pb_b1_z1"] = [1, 2, 3]
at.run()
assert not at.exception, at.exception
assert at.session_state["pb_b1_z1"] == [1, 2, 3], at.session_state["pb_b1_z1"]
print("  [2] 手改後保留不被覆蓋 OK")

# 3) 哨兵不符（模擬新一期）→ 重新種子
at.session_state["pb_seeded_id"] = "__stale__"
at.run()
assert not at.exception, at.exception
assert at.session_state["pb_b1_z1"] == pb_latest_z1, at.session_state["pb_b1_z1"]
print("  [3] 新一期重新種子 OK")

# 4) 重設按鈕：手改 → 按鈕 → 復原
at.session_state["pb_b1_z1"] = [7, 8, 9]
at.run()
assert at.session_state["pb_b1_z1"] == [7, 8, 9]
at.button(key="pb_reset").click().run()
assert not at.exception, at.exception
assert at.session_state["pb_b1_z1"] == pb_latest_z1, at.session_state["pb_b1_z1"]
print("  [4] 重設按鈕復原 OK")

print("SEED VERIFY ALL OK")
```

- [ ] **Step 2: 跑驗證**

Run: `.venv\Scripts\python.exe _seed_verify.py`
Expected:
```
  [1] 全新啟動種子化 OK
  [2] 手改後保留不被覆蓋 OK
  [3] 新一期重新種子 OK
  [4] 重設按鈕復原 OK
SEED VERIFY ALL OK
```

如出現 MISMATCH / exception：
- `[1]` 失敗 → 檢查 Task 3 的 `seed_inputs` 呼叫是否在 widget 建立前、key_prefix 是否正確
- `[2]` 失敗 → 哨兵比對邏輯錯（應 `!=` 才種子）
- `[4]` 失敗 → `reset_seed` 的 args/on_click 綁定錯

- [ ] **Step 3: 刪除暫存腳本（不 commit）**

Run: `Remove-Item _seed_verify.py`

- [ ] **Step 4: 更新 CLAUDE.md 進度快照**

在 `CLAUDE.md` 的「**大樂透分頁 ✅ 已完成上線**」段落**之後**、`**未完成 / 待優化（低優先）：**` 之前，插入：

```markdown

**自動帶入號碼 ✅ 已完成（2026-05-16）**

- 開啟威力彩/大樂透分頁自動帶入最新一期（第一區塊）+ 上一期（第二區塊），分析自動呈現
- 機制：`query_engine.latest_two_draws` + `ui_shared.seed_inputs`（draw_id 哨兵：同期只種一次、新期才覆蓋）
- 手改後不被覆蓋；每個 tab 有「↻ 帶回最新期」按鈕；caption 標示帶入的兩期
- 設計：`docs/superpowers/specs/2026-05-16-自動帶入號碼-design.md`
```

- [ ] **Step 5: Commit + push**

```bash
git add CLAUDE.md
git commit -m "docs: 自動帶入號碼功能完成，更新進度快照"
git push origin main
```

---

## 完工標準

- [ ] `latest_two_draws` 威力彩/大樂透回傳正確最新/上一期（含 draw_id）
- [ ] 全新開啟兩分頁：第一區塊=最新期、第二區塊=上一期，分析自動呈現
- [ ] 手改號碼 → 互動/重跑 → 不被覆蓋
- [ ] 「↻ 帶回最新期」按鈕把 4 個輸入梳回最新/上一期
- [ ] 模擬新一期（哨兵不符）→ 自動重新帶入
- [ ] caption 正確顯示帶入兩期；空表顯示提示
- [ ] AppTest 全程無 exception；威力彩既有分析行為不變
- [ ] CLAUDE.md 進度快照更新、push 完成
